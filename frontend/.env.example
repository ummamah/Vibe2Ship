# Frontend env template for local dev. Copy to `.env` (gitignored).
# Auth is opt-in. Leave VITE_AUTH_ENABLED=false for open-access runs.
VITE_AUTH_ENABLED=false
VITE_API_BASE_URL=http://localhost:8000/v1
